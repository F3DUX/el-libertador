'use client';

/**
 * TimelineBar — legacy component (now integrated into HeaderStatsBar).
 * Kept as a no-op to avoid import errors in case it's referenced elsewhere.
 */
export const TimelineBar = () => null;
